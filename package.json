{
  "name": "mudanza",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "dev:api": "pnpm --filter @mudanza/api run dev",
    "dev:mobile": "pnpm --filter @mudanza/mobile run start",
    "seed": "pnpm --filter @mudanza/api run seed"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
